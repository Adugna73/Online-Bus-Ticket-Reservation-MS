// test moved to .tsx variant (JSX requires .tsx)
import { it } from 'vitest'

// keep an inert skipped test so Vitest doesn't treat this file as an empty test file
it.skip('moved to .tsx - placeholder', () => {})


